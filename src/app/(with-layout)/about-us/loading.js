import Loader from "@/components/shared/Loader";

const Loading = () => {
  return (
    <div className="flex justify-center items-center my-32 font-bold">
      <Loader />
    </div>
  );
};

export default Loading;
